[phases.setup]
# needed to compile better-sqlite3 native addon
nixPkgs = ["python3", "gcc", "gnumake"]

[phases.build]
cmds = [
  "npm run seed",
  "npm run build"
]

[start]
cmd = "node apps/demo/dist/server/entry.mjs"
