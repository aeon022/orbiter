[phases.setup]
# nodejs_20 = Node + npm; python3/gcc/gnumake needed for better-sqlite3 native addon
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.build]
cmds = [
  "npm run seed",
  "npm run build"
]

[start]
cmd = "node apps/demo/dist/server/entry.mjs"
